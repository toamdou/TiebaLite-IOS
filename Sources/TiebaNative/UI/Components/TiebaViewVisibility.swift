// ============================================================
// 视图可见性：口径只此一处（"这个视图现在在屏幕上吗"）。
//
// 全 App 原先散着 8 处 `window != nil`，各写各的；收拢后改口径只需改这里，
// 也避免新代码再判出第四种写法（如 superview 链、isHidden、alpha 混用）。
// ============================================================

import UIKit

extension UIView {
  /// 在屏幕上吗？口径 = 已挂窗。被 push 盖住、切后台、未上屏都算离屏。
  /// 判据只用于"该不该降档/暂停"这类保守决策，不用于正确性。
  var tiebaIsOnScreen: Bool { window != nil }
}
